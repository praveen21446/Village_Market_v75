# v87 Mobile Cart Quantity Single Row Fix

On phone screens the buyer cart quantity control is forced to one non-wrapping horizontal row:

`[ − ] [ quantity ] [ + ] kg`

The minus, quantity input, and plus controls use matching 42x42px dimensions. The stylesheet cache version was also bumped so phones receive the updated CSS immediately after deployment.
